import RoomActions from "./actions";

const initState = {
  rooms: [],
  error: null,
};

const Reducer = (state = initState, action) => {
  switch (action.type) {
    
    default:
      return state;
  }
};

export default Reducer;
